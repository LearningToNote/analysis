SET SCHEMA "LEARNING_TO_NOTE";
DROP TABLE POS_TAGS;
CREATE COLUMN TABLE POS_TAGS(ID INTEGER GENERATED BY DEFAULT AS IDENTITY, POS VARCHAR(255));

INSERT INTO POS_TAGS(POS)
	SELECT DISTINCT TA_TYPE FROM "LEARNING_TO_NOTE"."$TA_FTI";

SELECT * FROM POS_TAGS;